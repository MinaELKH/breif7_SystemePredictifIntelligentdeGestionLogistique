# PySpark for big data processing
pyspark==3.5.0

# MongoDB connector for data storage
pymongo==4.6.0

# Data manipulation and analysis
pandas==2.1.4
numpy==1.26.2

matplotlib==3.8.2
seaborn==0.13.0

# Optional: For MongoDB integration with PySpark (if needed later)
# Note: pyspark[mongo] requires additional Java dependencies
# which are better managed through Spark packages

# Python version requirement: Python 3.8+